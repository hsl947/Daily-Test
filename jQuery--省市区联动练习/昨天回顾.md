1. jQuery的元素选择器

    ```
    .next() 
    .prev()
    .nextAll()
    .prevAll()
    .find()
    .eq() ///获取指定索引值的元素
    .parent()
    .children()
    .parents()
    .css()
    .siblings()
    .filter()
    $(window).height()
    $(window).width()
    .each()
    .offset({left:xx,top:xx}) ///偏移 
    ```